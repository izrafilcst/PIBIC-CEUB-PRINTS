# Tampa com entrada USB-C para o TP4056 — Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A `caixa-tampa` perde os 4 pinos da perfboard e ganha uma janela
passante para o plugue USB-C e um berço de encaixe (duas colunas em U com
garra) para um módulo TP4056 tipo C em pé — sem mudar um vértice do
`caixa-corpo`, que já está impresso.

**Architecture:** Tudo sai de `gerar_modelo_3mf.py`, fonte única da geometria:
um dicionário de parâmetros `CARREGADOR`, uma função de cotas derivadas
`estacao_carregador()`, as regras novas em `conferir_projeto()` e uma
`tampa()` reescrita, construída só com prismas em z sobre o núcleo `malha.py`.
`validar_modelo.py` relê o `.3mf` e sonda o berço; `gerar_desenhos_3mf.py`
mede a malha e redesenha a prancha CX-03.

**Tech Stack:** Python 3, só biblioteca padrão; pytest.

**Spec:** `docs/superpowers/specs/2026-09-24-tampa-tp4056-design.md` — leia
antes de começar. As seções citadas abaixo (§2.2, §3.4…) são dela.

## Global Constraints

- **O corpo não pode mudar.** O hash da malha do `caixa-corpo.3mf` tem de ser
  `c5e58c07c79b3695694d70010ca6ac83947300aeeafa0bdd023895d284806e0e` do
  começo ao fim (Task 1 cria o teste que garante isso).
- **Só biblioteca padrão.** Nada de numpy, trimesh, OpenSCAD.
- **Nada de cone nem rampa na geometria.** `Solido.conferir()` casa o volume
  da malha com um valor analítico em `tol_rel=1e-9`, e só fecha com área de
  POLÍGONO. Toda entrada é escalonada, todo alívio é prisma reto.
- **Volume esperado escrito à parte.** O analítico da tampa usa fórmula
  fechada para retângulos e colunas, e `area_assinada` só para os círculos.
- **Toda regra nova ganha um teste de perturbação**: o teste estraga UM
  parâmetro e exige que `conferir_projeto()` reprove com a mensagem certa.
- **Toda sonda nova é testada por mutação na MALHA**, nunca na expectativa.
- **Estilo do código:** comentários e docstrings em português SEM acento
  (`secao`, `nao`), como o resto do repositório. Texto que vai para a prancha
  SVG leva acento normalmente.
- **Commits** em português, no imperativo ("Acrescenta…", "Remove…"),
  terminando com a linha
  `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.
- **Não escreva arquivo por heredoc do bash com barra invertida dentro**: o
  shell desta máquina come a barra (`\n` vira quebra de linha real). Use as
  ferramentas Write/Edit.
- **`.3mf` e `.stl` só entram em commit na Task 8.** Rodar
  `gerar_modelo_3mf.py` antes disso é permitido, mas desfaça com
  `git checkout -- '*.3mf' '*.stl'` antes de cada commit.
- **Rode os testes com** `python -m pytest -q` na raiz do repositório.

## Mapa de arquivos

| Arquivo | O que muda |
|---|---|
| `tests/test_corpo_intocado.py` | **novo** — hash da malha do corpo |
| `malha.py` | + `retangulo`; − `perna`, `degrau_da_perna`, `_arco_cortado`, `_corda` |
| `gerar_modelo_3mf.py` | + `CARREGADOR`, `estacao_carregador`, `secao_coluna`, `ranhura`, regras; `tampa()` reescrita; − `PLACA`, `pinos_placa`, `ALT_COMPONENTE`, regras da placa |
| `tests/test_carregador.py` | **novo** — cotas derivadas e polígonos do berço |
| `tests/test_projeto.py` | + 8 perturbações e 2 testes da tampa; − 4 testes dos pinos |
| `tests/test_malha.py` | + teste de `retangulo`; − 2 testes da perna |
| `validar_modelo.py` | + `cruza_y`, `caixa_de`, `medir_berco`, `conferir_berco`; − `conferir_pinos` |
| `tests/test_validador.py` | **novo** — mutação das sondas do berço |
| `gerar_desenhos_3mf.py` | CX-03 reescrita; − sondas e detalhe do pino |
| `README.md`, `LISTA-DE-MATERIAIS.md`, specs | documentação |

**Ordem e janela vermelha:** depois da Task 4, `validar_modelo.py` e
`gerar_desenhos_3mf.py` quebram (ainda citam `P.PLACA`) até as Tasks 6 e 7.
O `pytest` fica verde em TODAS as tarefas.

---

### Task 1: Guarda do corpo impresso

**Files:**
- Create: `tests/test_corpo_intocado.py`

**Interfaces:**
- Consumes: `P.corpo()`, `M.salvar_3mf(caminho, solido, titulo)`, `V.ler_3mf(caminho) -> (V, T)`
- Produces: `hash_malha(Vs, Ts) -> str` (só dentro do teste)

Este teste é de caracterização: passa desde o primeiro minuto, e é por isso
que ele tem de ser provado por mutação no Step 3.

- [ ] **Step 1: Escreva o teste**

```python
# -*- coding: utf-8 -*-
"""
O corpo ja foi impresso. Esta revisao so pode reimprimir a tampa, entao a
malha do corpo nao pode mudar um vertice - nem por efeito colateral de mexer
numa constante que as duas pecas compartilham.

O hash e da MALHA (vertices e triangulos, na precisao gravada no .3mf), nao do
arquivo: o .3mf leva metadados que mudam a cada gravacao sem mudar a peca.
"""

import hashlib
import sys
from pathlib import Path

RAIZ = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(RAIZ))

import gerar_modelo_3mf as P
import malha as M
import validar_modelo as V

# malha do caixa-corpo.3mf de 88db49b - identica ate 0ba9eb1, e a impressa
REFERENCIA = "c5e58c07c79b3695694d70010ca6ac83947300aeeafa0bdd023895d284806e0e"


def hash_malha(Vs, Ts):
    h = hashlib.sha256()
    for v in Vs:
        h.update(("%.6f %.6f %.6f\n" % tuple(v)).encode())
    for t in Ts:
        h.update(("%d %d %d\n" % tuple(t)).encode())
    return h.hexdigest()


def test_o_corpo_gerado_agora_e_o_corpo_impresso(tmp_path):
    S, esperado = P.corpo()
    S.conferir(esperado, tol_rel=1e-9)
    arq = tmp_path / "corpo.3mf"
    M.salvar_3mf(str(arq), S, titulo="caixa-corpo")
    assert hash_malha(*V.ler_3mf(str(arq))) == REFERENCIA


def test_o_arquivo_do_corpo_no_repositorio_e_o_impresso():
    assert hash_malha(*V.ler_3mf(str(RAIZ / "caixa-corpo.3mf"))) == REFERENCIA
```

- [ ] **Step 2: Rode e confira que passa**

Run: `python -m pytest -q tests/test_corpo_intocado.py`
Expected: `2 passed`

- [ ] **Step 3: Prove que a guarda não é vazia**

Em `gerar_modelo_3mf.py` troque `PAIN_ESP = 8.0` por `PAIN_ESP = 7.5`.
Run: `python -m pytest -q tests/test_corpo_intocado.py::test_o_corpo_gerado_agora_e_o_corpo_impresso`
Expected: FAIL no `assert hash_malha(...) == REFERENCIA`.
Desfaça: `git checkout -- gerar_modelo_3mf.py`, rode de novo, `1 passed`.

- [ ] **Step 4: Commit**

```bash
git add tests/test_corpo_intocado.py
git commit -m "Acrescenta a guarda do corpo impresso: hash da malha" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 2: Parâmetros, cotas derivadas e polígonos do berço

**Files:**
- Modify: `malha.py` (nova função logo depois de `circulo`)
- Modify: `gerar_modelo_3mf.py` (bloco novo logo depois do fim de `estacao_chave()`, antes de `def pinos_placa`)
- Create: `tests/test_carregador.py`
- Modify: `tests/test_malha.py` (teste novo no fim)

**Interfaces:**
- Produces:
  - `M.retangulo(cx, cy, lx, ly) -> list[(x, y)]`, anti-horário, `lx` ao longo de x.
  - `P.CARREGADOR: dict` (chaves na Step 3).
  - `P.estacao_carregador() -> dict` com tudo de `CARREGADOR` mais: `yc, canal_w, w_fundo, w_ponta, w_fora, u_meia, x_usb, x_mod (x0, x1), z_garra, perfil [(z0, z1, g)], topo, niveis, cobre, alivio (ly, lx), envelope (x0, y0, x1, y1), z_topo_corpo, c_fibra, braco, eps`. Convenção: `janela`, `alivio` e `capa` são `(ao longo de y, ao longo de x)`; `g` é a profundidade do canal, `0.0` na garra.
  - `P.secao_coluna(e, g, lado) -> list[(x, y)]`, anti-horário; `lado=+1` coluna de y maior, `-1` a de y menor.
  - `P.ranhura(e, g0, g1, lado) -> list[(x, y)]`, faixa do canal entre as profundidades `g0 < g1`.

- [ ] **Step 1: Escreva os testes que falham**

Crie `tests/test_carregador.py`:

```python
# -*- coding: utf-8 -*-
"""Cotas derivadas e poligonos do berco do TP4056 - PIBIC-CEUB."""

import math
import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

import gerar_modelo_3mf as P
import malha as M


def test_estacao_tem_as_cotas_da_spec():
    e = P.estacao_carregador()
    esperado = dict(x_usb=165.55, yc=65.0, canal_w=2.0, w_fundo=8.7,
                    w_ponta=7.5, w_fora=10.7, u_meia=2.2, z_garra=32.2,
                    topo=34.2, cobre=1.0, z_topo_corpo=32.8, braco=28.8)
    for k, v in esperado.items():
        assert abs(e[k] - v) < 1e-9, f"{k} = {e[k]}, a spec diz {v}"
    assert e["alivio"] == pytest.approx((14.0, 8.2))
    assert e["envelope"] == pytest.approx((163.9, 54.3, 170.2, 75.7))
    assert e["niveis"] == [0.4, 0.8, 1.2]
    assert [g for _, _, g in e["perfil"]] == [1.2, 0.0, 0.4, 0.8]
    assert abs(e["eps"] - 0.0078363) < 1e-6


def test_secao_da_coluna_tem_a_area_da_formula_fechada():
    e = P.estacao_carregador()
    um, wp, wf, cw = e["u_meia"], e["w_ponta"], e["w_fora"], e["canal_w"]
    for g in (0.0, 0.4, 0.8, 1.2):
        for lado in (1, -1):
            A = M.area_assinada(P.secao_coluna(e, g, lado))
            assert A > 0, "a secao tem de sair anti-horaria"
            assert abs(A - (2 * um * (wf - wp) - cw * g)) < 1e-9


def test_as_colunas_sao_espelho_uma_da_outra_em_y():
    e = P.estacao_carregador()
    cima = sorted(P.secao_coluna(e, 1.2, 1))
    baixo = sorted((x, 2 * e["yc"] - y) for x, y in P.secao_coluna(e, 1.2, -1))
    assert len(cima) == len(baixo)
    assert all(math.dist(a, b) < 1e-9 for a, b in zip(cima, baixo))


def test_o_lado_do_canal_carrega_os_niveis_mais_rasos():
    """
    A emenda entre secoes so fecha se o lado do canal tiver um vertice em
    cada nivel mais raso que ele - o mesmo T da corda do pino.
    """
    e = P.estacao_carregador()
    x_lado = e["x"] - e["canal_w"] / 2
    ws = sorted(y - e["yc"] for x, y in P.secao_coluna(e, 1.2, 1)
                if abs(x - x_lado) < 1e-9)
    assert ws == pytest.approx([7.5, 7.9, 8.3, 8.7])


def test_ranhura_entre_a_boca_e_o_fundo():
    e = P.estacao_carregador()
    r = P.ranhura(e, 0.0, 1.2, 1)
    assert abs(M.area_assinada(r) - e["canal_w"] * 1.2) < 1e-9
    assert len(r) == 8      # 4 cantos + os niveis 0,4 e 0,8 dos dois lados
```

No fim de `tests/test_malha.py`:

```python
def test_retangulo_e_anti_horario_e_tem_a_area_certa():
    r = M.retangulo(10.0, 5.0, 4.0, 2.0)
    assert abs(M.area_assinada(r) - 8.0) < 1e-12
    assert min(p[0] for p in r) == 8.0 and max(p[0] for p in r) == 12.0
    assert min(p[1] for p in r) == 4.0 and max(p[1] for p in r) == 6.0
```

- [ ] **Step 2: Rode e confira que falham**

Run: `python -m pytest -q tests/test_carregador.py tests/test_malha.py`
Expected: FAIL com `AttributeError: module 'gerar_modelo_3mf' has no attribute 'estacao_carregador'` e `... 'malha' has no attribute 'retangulo'`.

- [ ] **Step 3: Implemente**

Em `malha.py`, logo depois de `def circulo(...)`:

```python
def retangulo(cx, cy, lx, ly):
    """Retangulo lx (em x) por ly (em y) centrado em (cx, cy), anti-horario."""
    return [(cx - lx / 2, cy - ly / 2), (cx + lx / 2, cy - ly / 2),
            (cx + lx / 2, cy + ly / 2), (cx - lx / 2, cy + ly / 2)]
```

Em `gerar_modelo_3mf.py`, logo depois do `return k` de `estacao_chave()`:

```python
# =====================================================================
# Carregador TP4056 tipo C - berco de encaixe na tampa
# =====================================================================
# ATENCAO: as cotas do modulo sao PRESUMIDAS - valores tipicos da placa com
# protecao (DW01 + 8205A), que variam de vendedor para vendedor. Meca antes de
# imprimir: 'comp' decide a folga sob a garra e 'larg' a folga do canal.
#
# O modulo fica EM PE: o USB-C e paralelo a placa, entao para o plugue subir
# de baixo para cima a placa tem de ficar perpendicular a tampa. Deitado, o
# conector apontaria para a parede do corpo, que nao pode ser reimpresso.
#
# Borda direita, entre o envelope do botao verde e a parede: e o unico bolsao
# longe do centro com 28 mm livres em z. Lado dos componentes para -x, a
# cavidade: o CI chega a 70-90 C carregando a 1 A e o PLA amolece a 55-60.
#
# 'y' e parametro, e nao CX_A/2, de proposito. A tampa e modelada espelhada
# (z invertido com o x, y do corpo), entao tudo nela tem de ser simetrico em
# y = CX_A/2 - e so um parametro livre deixa a conferencia provar isso.

CARREGADOR = dict(
    modelo="TP4056 tipo C, com protecao",
    comp=28.0, larg=17.0, esp=1.6,     # placa - PRESUMIDOS
    usb_l=9.0, usb_h=3.3,              # conector: largura e altura - PRESUMIDOS
    alt_comp=1.8,                      # mais alto alem do USB - PRESUMIDO
    capa=(12.35, 6.5),                 # capa do plugue, maximo da norma USB-C
    x=168.0, y=65.0,                   # plano medio da placa e centro dela
    folga=0.2,                         # encaixe em PLA, por lado
    prof_canal=1.2, aba=1.2, alma=2.0,
    garra_h=1.2,
    degraus=((0.4, 0.4), (0.8, 0.4)),  # entrada: (fundo do canal, altura)
    janela=(13.0, 7.2),                # (ao longo de y, ao longo de x)
)


def estacao_carregador():
    """
    Cotas derivadas do berco. x, y da peca; z da TAMPA (0 na face externa).

    Em planta cada coluna e um retangulo 2*u_meia (x) por w_fora - w_ponta
    (y), com um canal de largura canal_w aberto para o centro. 'w' e a
    distancia a y = yc: a ponta das abas fica em w_ponta, o fundo do canal em
    w_ponta + g. O perfil em z e a lista (z0, z1, g), de baixo para cima; na
    garra g = 0 e o canal fecha.
    """
    c = dict(CARREGADOR)
    c["yc"] = c["y"]
    c["canal_w"] = c["esp"] + 2 * c["folga"]                  # 2,00
    c["w_fundo"] = c["larg"] / 2 + c["folga"]                 # 8,70
    c["w_ponta"] = c["w_fundo"] - c["prof_canal"]             # 7,50
    c["w_fora"] = c["w_fundo"] + c["alma"]                    # 10,70
    c["u_meia"] = c["canal_w"] / 2 + c["aba"]                 # 2,20
    c["x_usb"] = c["x"] - c["esp"] / 2 - c["usb_h"] / 2       # 165,55
    c["x_mod"] = (c["x"] - c["esp"] / 2 - max(c["usb_h"], c["alt_comp"]),
                  c["x"] + c["esp"] / 2)
    c["z_garra"] = TAMPA_ESP + c["comp"] + c["folga"]         # 32,20
    perfil = [(TAMPA_ESP, c["z_garra"], c["prof_canal"]),
              (c["z_garra"], c["z_garra"] + c["garra_h"], 0.0)]
    z = c["z_garra"] + c["garra_h"]
    for g, h in c["degraus"]:
        perfil.append((z, z + h, g))
        z += h
    c["perfil"], c["topo"] = perfil, z                        # topo 34,20
    c["niveis"] = sorted({g for _, _, g in perfil if g > 0})
    c["cobre"] = c["prof_canal"] - c["folga"]                 # 1,00
    c["alivio"] = (c["janela"][0] + FOLGA_ALIVIO,
                   c["janela"][1] + FOLGA_ALIVIO)
    c["envelope"] = (min(c["x_mod"][0], c["x"] - c["u_meia"]),
                     c["yc"] - c["w_fora"],
                     max(c["x_mod"][1], c["x"] + c["u_meia"]),
                     c["yc"] + c["w_fora"])
    # topo do berco em z do CORPO: a face interna da tampa encosta em CORPO_H
    c["z_topo_corpo"] = CORPO_H + TAMPA_ESP - c["topo"]       # 32,80

    # Deformacao ao encaixar: a coluna inteira flete em y, engastada na
    # chapa. Secao U - alma de 'alma' mais duas abas de 'aba' x 'prof_canal'
    # - com a linha neutra calculada; c e a fibra mais distante dela. A
    # deflexao e a garra inteira (placa encostada no fundo DESTE canal).
    A_alma = 2 * c["u_meia"] * c["alma"]
    A_aba = 2 * c["aba"] * c["prof_canal"]
    ln = ((A_alma * c["alma"] / 2
           + A_aba * (c["alma"] + c["prof_canal"] / 2)) / (A_alma + A_aba))
    c["c_fibra"] = max(ln, c["alma"] + c["prof_canal"] - ln)
    c["braco"] = c["z_garra"] + c["garra_h"] / 2 - TAMPA_ESP  # 28,80
    c["eps"] = 3 * c["prof_canal"] * c["c_fibra"] / c["braco"] ** 2
    return c


def _coluna_local(e, g):
    """
    Contorno da coluna de cima em (u, w) locais: u = x - e['x'],
    w = y - yc. Anti-horario.

    O lado do canal recebe um vertice em CADA nivel mais raso que g. Sem
    isso a aresta unica de uma secao encontraria as varias da vizinha na
    emenda, e a malha abriria - o mesmo T da corda do pino.
    """
    hc, um = e["canal_w"] / 2, e["u_meia"]
    wp, wf = e["w_ponta"], e["w_fora"]
    lados = [d for d in e["niveis"] if d < g]
    pts = [(-um, wp), (-hc, wp)]
    if g > 0:
        pts += [(-hc, wp + d) for d in lados] + [(-hc, wp + g), (hc, wp + g)]
        pts += [(hc, wp + d) for d in reversed(lados)]
    pts += [(hc, wp), (um, wp), (um, wf), (-um, wf)]
    return pts


def _no_lugar(e, pts, lado):
    """(u, w) locais -> (x, y) da peca. lado = -1 espelha em y = yc."""
    return M.antihorario([(e["x"] + u, e["yc"] + lado * w) for u, w in pts])


def secao_coluna(e, g, lado):
    """Secao em planta de uma coluna do berco, com canal de fundo g."""
    return _no_lugar(e, _coluna_local(e, g), lado)


def ranhura(e, g0, g1, lado):
    """
    Faixa do canal entre as profundidades g0 < g1: a face horizontal que
    aparece onde a secao da coluna muda de um trecho para o seguinte.
    """
    hc, wp = e["canal_w"] / 2, e["w_ponta"]
    meio = [d for d in e["niveis"] if g0 < d < g1]
    pts = ([(-hc, wp + g0), (hc, wp + g0)] + [(hc, wp + d) for d in meio]
           + [(hc, wp + g1), (-hc, wp + g1)]
           + [(-hc, wp + d) for d in reversed(meio)])
    return _no_lugar(e, pts, lado)
```

- [ ] **Step 4: Rode e confira que passam**

Run: `python -m pytest -q`
Expected: `24 passed` (16 de antes + 2 da Task 1 + 6 novos).

- [ ] **Step 5: Commit**

```bash
git add malha.py gerar_modelo_3mf.py tests/test_carregador.py tests/test_malha.py
git commit -m "Acrescenta os parametros e os poligonos do berco do TP4056" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 3: As 9 regras do carregador em `conferir_projeto()`

**Files:**
- Modify: `gerar_modelo_3mf.py` — helper novo antes de `def conferir_projeto`; bloco novo dentro dela, logo antes de `if msgs:`; o `return` ganha a chave `carregador`
- Modify: `tests/test_projeto.py` — 8 testes novos no fim

**Interfaces:**
- Consumes: `estacao_carregador()`, `silhueta(reducao)`, `pontos_fixacao()`, `BOTOES`, `M.dentro(p, poly)`
- Produces: `conferir_projeto()["carregador"]` = o dict de `estacao_carregador()`; mensagens contendo `botao verde`, `parede`, `capa`, `conector`, `apoia`, `deforma`, `garra`, `simetric`.

As regras 1–9 são as da §5 da spec. Os valores de perturbação abaixo foram
conferidos num protótipo: cada um dispara a SUA regra.

- [ ] **Step 1: Escreva os testes que falham**

No fim de `tests/test_projeto.py`:

```python
# ---- estacao do carregador (TP4056) ----

def test_carregador_sobre_o_botao_verde_e_reprovado(restaurar):
    restaurar("CARREGADOR", dict(P.CARREGADOR, x=150.0))
    with pytest.raises(AssertionError, match="botao verde"):
        P.conferir_projeto()


def test_berco_encostado_na_parede_e_reprovado(restaurar):
    restaurar("CARREGADOR", dict(P.CARREGADOR, x=172.5))
    with pytest.raises(AssertionError, match="parede"):
        P.conferir_projeto()


def test_janela_que_nao_passa_a_capa_do_plugue_e_reprovada(restaurar):
    restaurar("CARREGADOR", dict(P.CARREGADOR, janela=(12.5, 7.2)))
    with pytest.raises(AssertionError, match="capa"):
        P.conferir_projeto()


def test_conector_largo_demais_para_a_janela_e_reprovado(restaurar):
    restaurar("CARREGADOR", dict(P.CARREGADOR, usb_l=11.5))
    with pytest.raises(AssertionError, match="conector"):
        P.conferir_projeto()


def test_placa_sem_apoio_na_chapa_e_reprovada(restaurar):
    restaurar("CARREGADOR", dict(P.CARREGADOR, larg=15.0))
    with pytest.raises(AssertionError, match="apoia"):
        P.conferir_projeto()


def test_garra_funda_demais_para_o_pla_e_reprovada(restaurar):
    # canal de 2,40: deflexao dobra e a fibra se afasta -> ~2,2 %
    restaurar("CARREGADOR", dict(P.CARREGADOR, prof_canal=2.4))
    with pytest.raises(AssertionError, match="deforma"):
        P.conferir_projeto()


def test_garra_que_nao_cobre_a_placa_e_reprovada(restaurar):
    restaurar("CARREGADOR", dict(P.CARREGADOR, folga=1.2))
    with pytest.raises(AssertionError, match="garra"):
        P.conferir_projeto()


def test_berco_fora_do_eixo_de_simetria_e_reprovado(restaurar):
    restaurar("CARREGADOR", dict(P.CARREGADOR, y=70.0))
    with pytest.raises(AssertionError, match="simetric"):
        P.conferir_projeto()
```

- [ ] **Step 2: Rode e confira que falham**

Run: `python -m pytest -q tests/test_projeto.py`
Expected: os 8 novos FAIL com `DID NOT RAISE`; os antigos passam.

- [ ] **Step 3: Implemente**

Logo antes de `def conferir_projeto():`

```python
def _dist_poligono(p, poly):
    """Menor distancia do ponto p ao contorno fechado poly."""
    melhor = float("inf")
    n = len(poly)
    for i in range(n):
        a, b = poly[i], poly[(i + 1) % n]
        dx, dy = b[0] - a[0], b[1] - a[1]
        t = max(0.0, min(1.0, ((p[0] - a[0]) * dx + (p[1] - a[1]) * dy)
                         / (dx * dx + dy * dy)))
        melhor = min(melhor, math.hypot(p[0] - a[0] - t * dx,
                                        p[1] - a[1] - t * dy))
    return melhor
```

Dentro de `conferir_projeto()`, logo antes de `if msgs:`:

```python
    # ---- estacao do carregador (TP4056) ----
    e = estacao_carregador()
    ex0, ey0, ex1, ey1 = e["envelope"]

    # 1. Contra cada botao: folga no plano OU em z, como a placa fazia.
    for b in BOTOES:
        d = math.hypot(max(ex0 - b["x"], 0.0, b["x"] - ex1),
                       max(ey0 - b["y"], 0.0, b["y"] - ey1)) - b["flange"] / 2
        dz = e["z_topo_corpo"] - b["abaixo"]
        exigir(d >= 2.0 or dz >= 2.0,
               f"carregador a {d:.2f} mm do botao {b['nome']} no plano e "
               f"{dz:.2f} mm em z - minimo 2,00")

    # 2. Tudo dentro da cavidade, a 1,00 da parede. A cavidade e convexa,
    #    entao basta conferir os cantos do envelope e do alivio.
    cav = silhueta(CORPO_PAR)
    ay_, ax_ = e["alivio"]
    cantos = ([(ex0, ey0), (ex1, ey0), (ex1, ey1), (ex0, ey1)]
              + [(e["x_usb"] + sx * ax_ / 2, e["yc"] + sy * ay_ / 2)
                 for sx in (-1, 1) for sy in (-1, 1)])
    for p in cantos:
        g = _dist_poligono(p, cav) * (1 if M.dentro(p, cav) else -1)
        exigir(g >= 1.0, f"berco do carregador a {g:.2f} mm da parede do "
                         f"corpo - minimo 1,00")

    # 3. Contra as colunas de inserto.
    for i, (cx, cy) in enumerate(fix, 1):
        d = math.hypot(max(ex0 - cx, 0.0, cx - ex1),
                       max(ey0 - cy, 0.0, cy - ey1)) - COL_D / 2
        exigir(d >= 2.0, f"berco do carregador a {d:.2f} mm da coluna {i} - "
                         f"minimo 2,00")

    # 4. A janela passa a capa do plugue, nao so o conector.
    jy, jx = e["janela"]
    cy_, cx_ = e["capa"]
    exigir(jy >= cy_ + 0.6 and jx >= cx_ + 0.6,
           f"janela {jy:.2f} x {jx:.2f} nao passa a capa do plugue "
           f"{cy_:.2f} x {cx_:.2f} com 0,30 por lado")

    # 5. O conector presumido tem margem dentro da janela.
    sobra = min((jy - e["usb_l"]) / 2, (jx - e["usb_h"]) / 2)
    exigir(sobra >= 1.0, f"conector sobra so {sobra:.2f} mm dentro da "
                         f"janela - minimo 1,00")

    # 6. As pontas da placa apoiam na chapa: e ela que leva o puxao.
    apoio = (e["larg"] - jy) / 2
    exigir(apoio >= 1.5, f"a placa apoia so {apoio:.2f} mm em cada ponta da "
                         f"janela - minimo 1,50")

    # 7. Deformacao da coluna ao encaixar.
    exigir(e["eps"] <= 0.010, f"deformacao da coluna do berco "
                              f"{e['eps']*100:.2f} % - maximo 1,00")

    # 8. A garra retem, e a entrada sobe do raso para o fundo.
    gs = [g for g, _ in e["degraus"]]
    exigir(e["cobre"] > 0 and all(0 < g < e["prof_canal"] for g in gs)
           and gs == sorted(set(gs)),
           f"a garra nao retem: cobre {e['cobre']:.2f} mm da placa, "
           f"degraus {gs}")

    # 9. Simetria em y: a tampa e modelada espelhada (spec, secao 2.2).
    exigir(abs(e["y"] - CX_A / 2) < 1e-9,
           f"berco em y = {e['y']:.2f}: a tampa e modelada espelhada e tudo "
           f"nela tem de ser simetrico em y = {CX_A / 2:.2f}")
```

No `return` do fim de `conferir_projeto()`, acrescente `carregador=e` ao
`dict(...)` (as outras chaves ficam como estão nesta tarefa).

- [ ] **Step 4: Rode e confira que passam**

Run: `python -m pytest -q`
Expected: `32 passed`.

- [ ] **Step 5: Commit**

```bash
git add gerar_modelo_3mf.py tests/test_projeto.py
git commit -m "Acrescenta as 9 regras de projeto do berco do TP4056" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 4: A tampa nova, e a saída dos pinos do modelo

**Files:**
- Modify: `gerar_modelo_3mf.py` — `tampa()` inteira; remove `PLACA`, o bloco de comentário acima dele, `ALT_COMPONENTE`, `pinos_placa()`, o bloco `# ---- perfboard ----` de `conferir_projeto()` (de `pl, pn = PLACA, pinos_placa()` até o `exigir(pl["farpa_d"] > ...)`), a chave `placa=` do `return`, e as linhas `pl = info["placa"]` / `print(f"placa centro ...")` de `main()`
- Modify: `tests/test_projeto.py`

**Interfaces:**
- Consumes: tudo da Task 2; `M.retangulo`
- Produces: `P.tampa() -> (Solido, volume_esperado)`; tampa com `z` de 0 a `e["topo"]` (34,20)

- [ ] **Step 1: Ajuste os testes**

Em `tests/test_projeto.py`:

1. Em `test_o_projeto_como_esta_passa`, troque
   `assert info["placa"]["centro"] == (73.0, 65.0)` por
   `assert info["carregador"]["x"] == 168.0`.
2. APAGUE `test_placa_batendo_no_botao_verde_e_reprovada`,
   `test_perna_curta_demais_e_reprovada`,
   `test_tampa_com_pinos_fecha_e_bate_o_volume` e
   `test_a_farpa_retem_a_placa`.
3. Acrescente no fim:

```python
def test_tampa_com_berco_fecha_e_bate_o_volume():
    S, esperado = P.tampa()
    vol = S.conferir(esperado, tol_rel=1e-9)
    e = P.estacao_carregador()
    zs = [v[2] for v in S.v]
    assert abs(max(zs) - e["topo"]) < 1e-9
    assert abs(min(zs)) < 1e-9
    assert abs(vol / 1000 - 81.97) < 0.01, "o prototipo da spec deu 81,97 cm3"


def test_a_tampa_nao_tem_mais_pinos():
    for nome in ("PLACA", "pinos_placa", "ALT_COMPONENTE"):
        assert not hasattr(P, nome), f"{nome} ainda existe"
    S, _ = P.tampa()
    x0, y0, x1, y1 = P.estacao_carregador()["envelope"]
    fora = [v for v in S.v if v[2] > P.TAMPA_ESP + 1e-9
            and not (x0 - 1e-9 <= v[0] <= x1 + 1e-9
                     and y0 - 1e-9 <= v[1] <= y1 + 1e-9)]
    assert fora == [], f"material acima da chapa fora do berco: {fora[:3]}"
```

- [ ] **Step 2: Rode e confira que falham**

Run: `python -m pytest -q tests/test_projeto.py`
Expected: FAIL nos dois novos (a tampa ainda tem os pinos: `max(zs)` 10,50;
`PLACA ainda existe`).

- [ ] **Step 3: Reescreva `tampa()`**

Substitua a função inteira por:

```python
def tampa():
    """
    z = 0 e a face EXTERNA, que vai na mesa; o berco do TP4056 cresce para
    cima a partir de z = TAMPA_ESP.

    Imprime com o berco PARA CIMA. O rebaixo Ø6,50 sobre o furo Ø3,40 fica
    voltado para baixo, mas sao 1,55 mm radiais sobre um vao de 6,50 - nao
    pede suporte. A janela do plugue ganha o mesmo alivio de boca dos furos
    do painel: ela nasce na primeira camada, e o pe de elefante estreitaria
    justo a passagem da capa.
    """
    S = M.Solido("tampa")
    ext = silhueta()
    fix = pontos_fixacao()
    e = estacao_carregador()

    pas = [M.circulo(x, y, D_PASSAGEM / 2, SEG_PEQ) for x, y in fix]
    cbo = [M.circulo(x, y, D_REBAIXO / 2, SEG_PEQ) for x, y in fix]
    jan = M.retangulo(e["x_usb"], e["yc"], e["janela"][1], e["janela"][0])
    ali = M.retangulo(e["x_usb"], e["yc"], e["alivio"][1], e["alivio"][0])
    bases = [secao_coluna(e, e["perfil"][0][2], lado) for lado in (1, -1)]

    # ---- chapa ----
    S.face(ext, cbo + [ali], 0.0, cima=False)
    for p, c in zip(pas, cbo):
        S.coroa(p, c, REB_TAMPA, cima=False)
        S.parede(c, 0.0, REB_TAMPA, fora=False)
        S.parede(p, REB_TAMPA, TAMPA_ESP, fora=False)
    S.parede(ali, 0.0, REB_ALIVIO, fora=False)
    S.face(ali, [jan], REB_ALIVIO, cima=False)
    S.parede(jan, REB_ALIVIO, TAMPA_ESP, fora=False)
    S.face(ext, pas + [jan] + bases, TAMPA_ESP, cima=True)
    S.parede(ext, 0.0, TAMPA_ESP, fora=True)

    # ---- berco: cada trecho do perfil e um prisma; entre dois trechos, a
    # faixa do canal que muda vira face - para cima onde o canal aprofunda,
    # para baixo onde ele fecha (a garra) ----
    pf = e["perfil"]
    for lado in (1, -1):
        for i, (z0, z1, g) in enumerate(pf):
            S.parede(secao_coluna(e, g, lado), z0, z1, fora=True)
            if i + 1 < len(pf):
                g_prox = pf[i + 1][2]
                if g_prox > g:
                    S.face(ranhura(e, g, g_prox, lado), [], z1, cima=True)
                else:
                    S.face(ranhura(e, g_prox, g, lado), [], z1, cima=False)
        S.face(secao_coluna(e, pf[-1][2], lado), [], pf[-1][1], cima=True)

    # Volume esperado por formula fechada: retangulo e coluna em U nao passam
    # pelos poligonos acima, so os circulos passam por area_assinada.
    A = lambda p: abs(M.area_assinada(p))
    um, wp, wf, cw = e["u_meia"], e["w_ponta"], e["w_fora"], e["canal_w"]
    esperado = (A(ext) * TAMPA_ESP
                - 6 * (A(cbo[0]) * REB_TAMPA
                       + A(pas[0]) * (TAMPA_ESP - REB_TAMPA))
                - e["alivio"][0] * e["alivio"][1] * REB_ALIVIO
                - e["janela"][0] * e["janela"][1] * (TAMPA_ESP - REB_ALIVIO)
                + 2 * sum((2 * um * (wf - wp) - cw * g) * (z1 - z0)
                          for z0, z1, g in pf))
    return S, esperado
```

- [ ] **Step 4: Remova os pinos do modelo**

Apague de `gerar_modelo_3mf.py`: o bloco de comentário
`# Perfboard 7 x 9 - encaixe por pino farpado na tampa` inteiro, `PLACA = dict(...)`,
`ALT_COMPONENTE = ...`, `def pinos_placa(): ...`, o bloco `# ---- perfboard ----`
de `conferir_projeto()` (termina no `exigir(pl["farpa_d"] > pl["furo_d"] > ...)`),
e a chave `placa=dict(...)` do `return`. Em `main()`, troque as duas linhas da
placa por:

```python
    c = info["carregador"]
    print(f"carregador {c['modelo']}  x={c['x']:.2f}  janela "
          f"{c['janela'][0]:.2f} x {c['janela'][1]:.2f}  topo {c['topo']:.2f}  "
          f"deformacao {c['eps']*100:.2f} %")
```

No docstring do topo do módulo, a linha
`caixa-tampa.3mf    tampa de servico,                        178 x 130 x 4`
passa a
`caixa-tampa.3mf    tampa de servico, com o berco do TP4056, 178 x 130 x 34,2`.

Confira que não sobrou nada: `grep -n "PLACA\|pinos_placa\|ALT_COMPONENTE\|perna\|farpa" gerar_modelo_3mf.py`
Expected: nenhuma linha.

- [ ] **Step 5: Rode e confira que passam**

Run: `python -m pytest -q`
Expected: `30 passed` (32 − 4 apagados + 2 novos).

Run: `python gerar_modelo_3mf.py`
Expected: `caixa-tampa ... 81,97 cm3  altura 4 mm` (a coluna "altura" imprime
`TAMPA_ESP`, da lista `PECAS`) e a linha `carregador TP4056 ...
deformacao 0,78 %`. Depois: `git checkout -- '*.3mf' '*.stl'`.

- [ ] **Step 6: Commit**

```bash
git add gerar_modelo_3mf.py tests/test_projeto.py
git commit -m "Reescreve a tampa: janela USB-C e berco do TP4056 no lugar dos pinos" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 5: Primitivas do pino saem do núcleo

**Files:**
- Modify: `malha.py` — apaga `_arco_cortado`, `_corda`, `perna`, `degrau_da_perna`
- Modify: `tests/test_malha.py` — apaga `test_perna_e_meia_secao_menos_a_fenda` e `test_degrau_da_perna_fecha_um_solido`

**Interfaces:**
- Consumes: nada. `tubo` e `face_vertical` FICAM — o corpo usa as duas.

- [ ] **Step 1: Confirme que ninguém mais usa**

Run: `grep -rn "perna\|_arco_cortado\|_corda" --include=*.py .`
Expected: só as definições em `malha.py`, os 2 testes em `tests/test_malha.py`,
e as referências em `validar_modelo.py`/`gerar_desenhos_3mf.py` se houver
(essas saem nas Tasks 6 e 7 — se aparecer uso real FORA de docstring nesses
dois, pare e reporte).

- [ ] **Step 2: Apague as quatro funções e os dois testes**

- [ ] **Step 3: Rode**

Run: `python -m pytest -q`
Expected: `28 passed`.

- [ ] **Step 4: Commit**

```bash
git add malha.py tests/test_malha.py
git commit -m "Remove do nucleo as primitivas que so os pinos usavam" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 6: Validador — sondas do berço, testadas por mutação

**Files:**
- Modify: `validar_modelo.py` — funções novas logo depois de `largura_do_vao`; `conferir_berco` no lugar de `conferir_pinos`; fim de `main()`
- Create: `tests/test_validador.py`

**Interfaces:**
- Consumes: `P.estacao_carregador()`, `P.tampa()`, e do próprio validador `secao`, `travessia`, `ok`, `falhas`
- Produces: `cruza_y(segs, y0) -> list[float]`, `caixa_de(segs, x0, x1, y0, y1) -> (x0, y0, x1, y1) | None`, `medir_berco(V, T) -> dict(alivio, janela, secoes=[dict(z, g, ys, xs=[xs_cima, xs_baixo])], topo)`, `conferir_berco(V, T)`. A Task 7 importa `medir_berco` e `travessia`.

- [ ] **Step 1: Escreva os testes que falham**

Crie `tests/test_validador.py`:

```python
# -*- coding: utf-8 -*-
"""
Mutacao das sondas do berco. Uma sonda que nunca reprovou nada nao prova
nada - foi assim que a sonda do pino concordou com tudo por uma revisao
inteira, porque 'eh_circulo' rejeitava a secao em D.

A mutacao e na MALHA, nunca na expectativa: a tampa e gerada com um
parametro estragado, o parametro volta ao lugar, e so entao a sonda roda.
Estragar o parametro e sondar com ele estragado nao testaria nada - malha e
expectativa sairiam do mesmo numero errado.
"""

import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

import gerar_modelo_3mf as P
import validar_modelo as V


def malha_com(carregador=None, **globais):
    guarda = {"CARREGADOR": P.CARREGADOR}
    guarda.update({k: getattr(P, k) for k in globais})
    try:
        P.CARREGADOR = dict(P.CARREGADOR, **(carregador or {}))
        for k, v in globais.items():
            setattr(P, k, v)
        S, esperado = P.tampa()
        S.conferir(esperado, tol_rel=1e-9)
    finally:
        for k, v in guarda.items():
            setattr(P, k, v)
    return [tuple(v) for v in S.v], list(S.t)


def falhas_do_berco(Vs, Ts):
    V.falhas = 0
    V.conferir_berco(Vs, Ts)
    return V.falhas


def test_a_tampa_como_esta_passa_nas_sondas():
    assert falhas_do_berco(*malha_com()) == 0


@pytest.mark.parametrize("carregador, globais", [
    (dict(janela=(12.0, 7.2)), {}),
    ({}, dict(FOLGA_ALIVIO=2.0)),
    (dict(folga=0.4), {}),
    (dict(degraus=((0.6, 0.4), (0.8, 0.4))), {}),
    (dict(comp=26.0), {}),
    (dict(x=167.0), {}),
], ids=["janela", "alivio", "canal", "degrau", "garra", "posicao"])
def test_cada_sonda_acusa_a_malha_estragada(carregador, globais):
    assert falhas_do_berco(*malha_com(carregador, **globais)) > 0
```

- [ ] **Step 2: Rode e confira que falham**

Run: `python -m pytest -q tests/test_validador.py`
Expected: FAIL com `AttributeError: module 'validar_modelo' has no attribute 'conferir_berco'`.

- [ ] **Step 3: Implemente**

Logo depois de `def largura_do_vao(...)`:

```python
def cruza_y(segs, y0):
    """
    Onde a reta horizontal y = y0 corta a secao, em x crescente. E a
    'travessia' com os eixos trocados - e com a mesma regra semiaberta, que e
    o que importa aqui: a sonda do meio do canal cai exatamente no ponto
    medio das arestas do fundo dele.
    """
    return travessia([((ay, ax), (by, bx)) for (ax, ay), (bx, by) in segs], y0)


def caixa_de(segs, x0, x1, y0, y1):
    """Caixa envolvente dos pontos da secao dentro de [x0, x1] x [y0, y1]."""
    pts = [p for s in segs for p in s if x0 <= p[0] <= x1 and y0 <= p[1] <= y1]
    if not pts:
        return None
    xs, ys = [p[0] for p in pts], [p[1] for p in pts]
    return min(xs), min(ys), max(xs), max(ys)


def medir_berco(V, T):
    """
    Janela, alivio e cada trecho das colunas do berco, medidos na secao.

    As alturas de corte vem do projeto; o que se mede e a malha. Cada trecho
    do perfil e cortado no meio da sua faixa de z, com duas sondas: a
    vertical no plano medio da placa (x = e['x']) acha o fundo do canal, ou
    a boca fechada na garra; a horizontal no meio do canal acha as abas. Um
    canal que sumisse, ou uma garra que nao fechasse, muda a CONTAGEM de
    cruzamentos - a sonda nao tem como concordar por omissao.

    A janela e cortada em (REB_TAMPA + TAMPA_ESP)/2 e nao a meia chapa: em
    z = 2,00 fica o fundo do rebaixo dos parafusos, e cortar sobre um degrau
    da malha e cortar sobre face horizontal.
    """
    e = P.estacao_carregador()
    xc, yc = e["x_usb"], e["yc"]
    r = {}
    for nome, z, (ly, lx) in (("alivio", P.REB_ALIVIO / 2, e["alivio"]),
                              ("janela", (P.REB_TAMPA + P.TAMPA_ESP) / 2,
                               e["janela"])):
        r[nome] = caixa_de(secao(V, T, z), xc - lx, xc + lx, yc - ly, yc + ly)
    r["secoes"] = []
    for z0, z1, g in e["perfil"]:
        s = secao(V, T, (z0 + z1) / 2)
        r["secoes"].append(dict(
            z=(z0 + z1) / 2, g=g, ys=travessia(s, e["x"]),
            xs=[cruza_y(s, yc + lado * (e["w_ponta"] + e["prof_canal"] / 2))
                for lado in (1, -1)]))
    r["topo"] = max(v[2] for v in V)
    return r


def conferir_berco(V, T):
    """O berco do TP4056 e a janela do plugue, contra estacao_carregador()."""
    e = P.estacao_carregador()
    r = medir_berco(V, T)
    print("\ncaixa-tampa.3mf - berco do TP4056")

    def perto(a, b):
        return abs(a - b) < 0.01

    for nome, (ly, lx) in (("alivio", e["alivio"]), ("janela", e["janela"])):
        c = r[nome]
        ok(c is not None and perto(c[2] - c[0], lx) and perto(c[3] - c[1], ly)
           and perto((c[0] + c[2]) / 2, e["x_usb"])
           and perto((c[1] + c[3]) / 2, e["yc"]),
           f"{nome} {ly:.2f} x {lx:.2f} em ({e['x_usb']:.2f}, {e['yc']:.2f})",
           "nao achada" if c is None else
           f"medida {c[3] - c[1]:.3f} x {c[2] - c[0]:.3f} em "
           f"({(c[0] + c[2]) / 2:.3f}, {(c[1] + c[3]) / 2:.3f})")

    x, yc = e["x"], e["yc"]
    hc, um = e["canal_w"] / 2, e["u_meia"]
    for sc in r["secoes"]:
        w = e["w_ponta"] + sc["g"]
        esp_ys = [yc - e["w_fora"], yc - w, yc + w, yc + e["w_fora"]]
        rotulo = ("garra fechada" if sc["g"] == 0
                  else f"canal de {sc['g']:.2f}")
        ok(len(sc["ys"]) == 4
           and all(perto(a, b) for a, b in zip(sc["ys"], esp_ys)),
           f"z {sc['z']:.2f}: {rotulo}, boca a {w:.2f} do centro",
           "cruzamentos em y " + ", ".join(f"{v:.3f}" for v in sc["ys"]))
        # a reta do meio do canal so cruza as abas onde o canal passa dela
        esp_xs = ([x - um, x - hc, x + hc, x + um]
                  if sc["g"] > e["prof_canal"] / 2 else [x - um, x + um])
        for lado, xs in zip((1, -1), sc["xs"]):
            ok(len(xs) == len(esp_xs)
               and all(perto(a, b) for a, b in zip(xs, esp_xs)),
               f"z {sc['z']:.2f}: coluna {'de cima' if lado > 0 else 'de baixo'}"
               f" em x {x - um:.2f}..{x + um:.2f}",
               "cruzamentos em x " + ", ".join(f"{v:.3f}" for v in xs))
    ok(perto(r["topo"], e["topo"]), f"topo do berco {e['topo']:.2f}",
       f"medido {r['topo']:.3f}")
```

Apague `conferir_pinos` inteira. No fim de `main()`, troque

```python
    pn = P.pinos_placa()
    V, T = conferir("tampa", "caixa-tampa.3mf", P.TAMPA_ESP + pn["topo"], f)
    conferir_pinos(V, T)
```

por

```python
    V, T = conferir("tampa", "caixa-tampa.3mf",
                    P.estacao_carregador()["topo"], f)
    conferir_berco(V, T)
```

No docstring do topo, acrescente depois da linha `parede ...`:
`  berco            janela do plugue e colunas do TP4056, por secao`.

- [ ] **Step 4: Rode e confira que passam**

Run: `python -m pytest -q`
Expected: `35 passed`.

Run: `python gerar_modelo_3mf.py && python validar_modelo.py`
Expected: termina em `todas as conferencias passaram`, com as linhas do
`berco do TP4056` todas `[ok ]`. Depois: `git checkout -- '*.3mf' '*.stl'`.

- [ ] **Step 5: Commit**

```bash
git add validar_modelo.py tests/test_validador.py
git commit -m "Troca as sondas do pino pelas do berco, com teste de mutacao" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 7: Prancha CX-03

**Files:**
- Modify: `gerar_desenhos_3mf.py` — import; `tabela_furos`; `_esp_chapa` (docstring); `_prof_rebaixo` e `_caixa` novas; `desenho_tampa()` inteira; `_detalhe_berco` nova no lugar de `_detalhe_pino`; apaga `_centros_pino`, `_diam_pino`, `_fenda_pino`

**Interfaces:**
- Consumes: `P.estacao_carregador()`, `validar_modelo.medir_berco`, e do próprio módulo `analisar`, `niveis`, `raio_canto`, `bate`, `Blocos`, `Vista`, `contorno`, `furo`, `rotulos`, `tabela_furos`, `_detalhe_furo`
- Produces: `desenho-caixa-tampa.svg`

A prancha foi prototipada contra a malha nova: com as origens abaixo as
conferências de cota e de layout passam, e o bloco de notas termina em
~530 de 558 mm úteis.

**Desvio registrado na spec (§7):** não há detalhe ampliado em planta. A
planta do berço fica na vista superior 1:1 — os dois U, a janela, o alívio e
o envelope do módulo em referência —, porque um segundo detalhe não cabe na
faixa livre da folha retrato.

- [ ] **Step 1: Confira o vermelho**

Run: `python gerar_modelo_3mf.py && python gerar_desenhos_3mf.py`
Expected: FAIL — `AttributeError: module 'gerar_modelo_3mf' has no attribute 'pinos_placa'`.

- [ ] **Step 2: Import e tabela**

Logo depois do `from gerar_desenhos import (...)`:

```python
from validar_modelo import medir_berco
```

Em `tabela_furos`, a linha
`linhas = [(n, vg(cx), vg(cy), dm(d)) for n, cx, cy, d in bloco]` passa a

```python
        # o 4o campo e diametro, ou ja o texto da medida (a janela e retangulo)
        linhas = [(n, vg(cx), vg(cy),
                   dm(d) if isinstance(d, (int, float)) else d)
                  for n, cx, cy, d in bloco]
```

No docstring de `_esp_chapa`, `Acima dela so existem os pinos, e o contorno
mais largo passa a ser uma perna.` passa a `Acima dela so existem as colunas
do berco, e o contorno mais largo passa a ser uma delas.`

- [ ] **Step 3: Medições novas**

Apague `_centros_pino`, `_diam_pino` e `_fenda_pino`. No lugar delas:

```python
def _prof_rebaixo(V, T, zs, esp):
    """
    Fundo do rebaixo dos parafusos, MEDIDO: a primeira faixa de z em que o
    furo de fixacao muda de raio. Nao da para usar zs[1] - o alivio da janela
    poe um nivel em 0,50 antes do fundo do rebaixo.
    """
    r0 = None
    for z0, z1 in zip(zs, zs[1:]):
        if z1 > esp:
            break
        _, f = analisar(V, T, (z0 + z1) / 2)
        r = max(x[2] for x in f)
        if r0 is None:
            r0 = r
        elif abs(r - r0) > 0.01:
            return z0
    return None


def _caixa(lo):
    xs, ys = [p[0] for p in lo], [p[1] for p in lo]
    return min(xs), min(ys), max(xs), max(ys)
```

- [ ] **Step 4: O detalhe do berço**

Apague `_detalhe_pino` inteira e ponha no lugar:

```python
def _detalhe_berco(D, ox, oy, e, esp, S=4.0):
    """
    O berco em corte, no plano medio da placa (x = e['x']), ampliado.

    O plano passa pelo meio do canal, entao a coluna aparece do fundo do
    canal ate a face de fora - e na garra, da boca fechada. Chapa e coluna
    saem num poligono so por lado: separados, uma linha de contorno
    atravessaria material continuo.

    A placa entra em linha de referencia: as cotas dela sao PRESUMIDAS, nao
    medidas. Os degraus de 0,40 somem a 4:1 (1,6 mm de papel) e vao para a
    nota em vez de cota.
    """
    wp, wf = e["w_ponta"], e["w_fora"]
    jh, ah, ra = e["janela"][0] / 2, e["alivio"][0] / 2, P.REB_ALIVIO
    ctx = 15.0                          # quanto de chapa aparece de cada lado

    def X(sg, w):
        return ox + sg * w * S

    def Y(z):
        return oy - z * S

    for sg in (-1, 1):
        pts = [(jh, esp), (wp + e["perfil"][0][2], esp)]
        for z0, z1, g in e["perfil"]:
            pts += [(wp + g, z0), (wp + g, z1)]
        pts += [(wf, e["topo"]), (wf, esp), (ctx, esp), (ctx, 0.0),
                (ah, 0.0), (ah, ra), (jh, ra)]
        D.hachura_poli([(X(sg, w), Y(z)) for w, z in pts])

    D.ret(ox - e["larg"] / 2 * S, Y(esp + e["comp"]), e["larg"] * S,
          e["comp"] * S, L_TRACO, cor=C_REF, dash=D_FANTASMA)
    D.txt(ox, Y(esp + e["comp"] / 2), "placa TP4056", FONTE_P, cor=C_REF)
    D.txt(ox, Y(esp + e["comp"] / 2) + 4, "(ref., PRESUMIDA)", FONTE_P,
          cor=C_REF)

    wg = wp + e["prof_canal"]
    D.cota_h(X(-1, wg), X(1, wg), Y(12.0), vg(2 * wg), tam=FONTE_P)
    D.cota_h(X(-1, wf), X(1, wf), Y(e["topo"]) - 10, vg(2 * wf),
             Y(e["topo"]), tam=FONTE_P)
    D.cota_h(X(-1, jh), X(1, jh), oy + 10, vg(2 * jh), oy, tam=FONTE_P)
    D.cota_h(X(-1, ah), X(1, ah), oy + 20, vg(2 * ah), oy, tam=FONTE_P)
    xd = X(1, wf)
    D.cota_v(Y(e["z_garra"]), Y(esp), xd + 10, vg(e["z_garra"] - esp), xd,
             tam=FONTE_P)
    D.cota_v(Y(e["topo"]), Y(0.0), xd + 22, vg(e["topo"]), X(1, ctx),
             tam=FONTE_P)
    D.cota_v(Y(esp + e["comp"]), Y(esp), X(-1, wf) - 12, vg(e["comp"]),
             X(-1, e["larg"] / 2), tam=FONTE_P)
    D.bandeira(X(1, wp), Y(e["z_garra"] + e["garra_h"] / 2), 6, ang=-150,
               comp=16)
    D.rotulo_vista(ox, oy + 32,
                   f"CORTE - berço no plano da placa (x = {vg(e['x'])})",
                   f"{S:.0f}:1")
```

- [ ] **Step 5: `desenho_tampa()` nova**

Substitua a função inteira por:

```python
def desenho_tampa():
    V, T = ler_3mf("caixa-tampa.3mf")
    zs = niveis(V)
    e = P.estacao_carregador()
    r = medir_berco(V, T)
    esp = _esp_chapa(V, T, zs)
    hreb = _prof_rebaixo(V, T, zs, esp)
    alt_total = zs[-1]

    # A chapa: corte a meia espessura do rebaixo e a meia espessura da
    # passagem. z = 0 e a face EXTERNA. No segundo corte a janela e o
    # segundo contorno nao circular - o primeiro e a silhueta.
    c_r, f_r = analisar(V, T, hreb / 2)
    c_p, f_p = analisar(V, T, (hreb + esp) / 2)
    lo = c_r[0]
    larg = max(p[0] for p in lo) - min(p[0] for p in lo)
    alt = max(p[1] for p in lo) - min(p[1] for p in lo)
    R = raio_canto(lo)
    rebaixos, passantes = f_r, f_p
    dp, dr = 2 * passantes[0][2], 2 * rebaixos[0][2]
    jx0, jy0, jx1, jy1 = _caixa(c_p[1])
    ax0, ay0, ax1, ay1 = _caixa(analisar(V, T, P.REB_ALIVIO / 2)[0][1])
    xj, yj = (jx0 + jx1) / 2, (jy0 + jy1) / 2
    z_canal = (e["perfil"][0][0] + e["perfil"][0][1]) / 2
    colunas = analisar(V, T, z_canal)[0]
    zb = [z for z in zs if z > esp + 1e-9]          # niveis do berco

    s0, s1 = r["secoes"][0], r["secoes"][1]
    q = lambda l, i, j: l[i] - l[j] if len(l) == 4 else None
    bate("tampa: espessura da chapa", esp, P.TAMPA_ESP)
    bate("tampa: largura", larg, P.CX_L)
    bate("tampa: profundidade", alt, P.CX_A)
    bate("tampa: raio de canto", R, P.CX_R)
    bate("tampa: rebaixo", hreb, P.REB_TAMPA)
    bate("tampa: Ø de passagem", dp, P.D_PASSAGEM)
    bate("tampa: Ø do rebaixo", dr, P.D_REBAIXO)
    bate("tampa: n de furos", len(rebaixos), 6)
    bate("tampa: janela em y", jy1 - jy0, e["janela"][0])
    bate("tampa: janela em x", jx1 - jx0, e["janela"][1])
    bate("tampa: x da janela", xj, e["x_usb"])
    bate("tampa: y da janela", yj, e["yc"])
    bate("tampa: alivio em y", ay1 - ay0, e["alivio"][0])
    bate("tampa: alivio em x", ax1 - ax0, e["alivio"][1])
    bate("tampa: n de colunas do berco", len(colunas), 2)
    bate("berco: canal", q(s0["xs"][0], 2, 1), e["canal_w"])
    bate("berco: vao entre os fundos", q(s0["ys"], 2, 1), 2 * e["w_fundo"])
    bate("berco: largura total", q(s0["ys"], 3, 0), 2 * e["w_fora"])
    bate("berco: boca na garra", q(s1["ys"], 2, 1), 2 * e["w_ponta"])
    bate("berco: face de baixo da garra", zb[0], e["z_garra"])
    bate("berco: face de cima da garra", zb[1], e["z_garra"] + e["garra_h"])
    bate("tampa: altura total", alt_total, e["topo"])

    L = esp - hreb + P.PENETRACAO

    D = Desenho(FOLHA_W, FOLHA_H, "CAIXA - TAMPA DE SERVIÇO",
                "Vista superior, furo de fixação e berço do carregador TP4056",
                escala="1:1 (ver ampliações)", codigo="PIBIC-CX-03")
    B = Blocos(D, "CX-03")

    # As duas ampliacoes empilham na faixa livre a direita da vista superior;
    # o texto fica com a folha inteira embaixo.
    with B("detalhe do furo"):
        _detalhe_furo(D, 334, 110, esp, hreb, dp, dr, L)
    with B("detalhe do berço"):
        _detalhe_berco(D, 334, 360, e, esp)

    # Vista de +z, a FACE INTERNA, como a peca sai da mesa: passagem, janela
    # e berco visiveis; rebaixo e alivio da janela, que ficam na face da
    # mesa, ocultos.
    B.abre("vista superior")
    W = Vista(46, 110, larg, alt)
    contorno(D, lo, W)
    for f in passantes:
        furo(D, W, f)
    for f in rebaixos:
        furo(D, W, f, marca=False, lw=L_TRACO, cor="#555", dash=D_OCULTA)
    contorno(D, c_p[1], W)
    D.ret(W.px(ax0), W.py(ay1), ax1 - ax0, ay1 - ay0, L_TRACO, cor="#555",
          dash=D_OCULTA)
    for lo_c in colunas:
        contorno(D, lo_c, W)
    D.ret(W.px(e["x_mod"][0]), W.py(e["yc"] + e["larg"] / 2),
          e["x_mod"][1] - e["x_mod"][0], e["larg"], L_TRACO, cor=C_REF,
          dash=D_FANTASMA)
    D.txt(W.px(e["x_mod"][0]) - 3, W.py(e["yc"] + e["larg"] / 2) - 2,
          "TP4056 (ref.)", FONTE_P, anc="end", cor=C_REF)

    yb = W.py(0)
    xs = sorted({round(f[0], 3) for f in rebaixos})
    ys = sorted({round(f[1], 3) for f in rebaixos})
    D.cota_h(W.px(0), W.px(xs[0]), yb + 14, vg(xs[0]), yb)
    D.cota_h(W.px(0), W.px(xj), yb + 26, vg(xj), yb)
    D.cota_h(W.px(0), W.px(larg), yb + 38, vg(larg), yb)
    D.cota_v(W.py(alt), yb, W.px(0) - 14, vg(alt), W.px(0))
    D.cota_v(W.py(yj), yb, W.px(larg) + 14, vg(yj), W.px(larg))
    D.bandeira(*W.p(xs[2], ys[2] + dr / 2), 1, ang=-45, comp=18)
    D.bandeira(W.px(larg - R * 0.293), W.py(alt - R * 0.293), 2, ang=-30,
               comp=18)
    D.bandeira(*W.p(xs[0], ys[0] - dr / 2), 4, ang=-135, comp=18)
    D.bandeira(*W.p(jx0, yj), 5, ang=-160, comp=22)
    D.bandeira(*W.p(e["x"], e["yc"] + e["w_fora"]), 6, ang=-120, comp=20)
    D.rotulo_vista(W.px(larg / 2), yb + 50,
                   "VISTA SUPERIOR - face interna, como sai da mesa", "1:1")
    B.fecha()

    B.abre("notas")
    y = D.notas(COL_DIR, Y_TEXTO, [
        (1, f"{len(rebaixos)}x {dm(dp)} passante com rebaixo {dm(dr)} x "
            f"{vg(hreb)} para a cabeça do parafuso M3 ISO 7380. O rebaixo "
            f"fica na face da mesa - oculto nesta vista."),
        (2, f"Silhueta idêntica à do corpo: {vg(larg)} x {vg(alt)}, cantos "
            f"R{vg(R)}."),
        (3, f"Chapa {vg(esp)}; sob a cabeça do parafuso restam "
            f"{vg(esp - hreb)} mm - é a seção mais fina da peça."),
        (4, f"Fixação com {len(rebaixos)}x M3 x {L:.0f} ISO 7380 em inserto de "
            f"latão M3 no topo das colunas do corpo (ver PIBIC-CX-02)."),
        (5, f"Janela do plugue USB-C {vg(jy1 - jy0)} x {vg(jx1 - jx0)} "
            f"passante, centro em x = {vg(xj)}, y = {vg(yj)}, com alívio de "
            f"boca {vg(ay1 - ay0)} x {vg(ax1 - ax0)} x {vg(P.REB_ALIVIO)} na "
            f"face da mesa."),
        (None, "MONTE COM A JANELA DO LADO DO BOTÃO VERDE. Girada 180°, a "
               "tampa ainda parafusa, mas o módulo bate no botão vermelho."),
        (6, f"Berço do TP4056: 2 colunas em U, canal {vg(e['canal_w'])} x "
            f"{vg(e['prof_canal'])}; a garra cobre {vg(e['cobre'])} da borda "
            f"de cima da placa, com {vg(e['folga'])} de folga."),
        (None, f"Entrada em {len(e['degraus'])} degraus retos de "
               f"{vg(e['degraus'][0][1])}, sem rampa. Altura total da peça "
               f"{vg(alt_total)}."),
        (7, f"Imprimir com a face externa NA MESA e o berço para cima. O "
            f"rebaixo dos parafusos ({vg((dr - dp) / 2)} radial sobre "
            f"{vg(dr)}) e o alívio da janela não pedem suporte."),
    ], titulo="NOTAS DE FABRICAÇÃO", larg=LARG_TEXTO)

    y = D.notas(COL_DIR, y + 4, [
        (None, f"As cotas do TP4056 são PRESUMIDAS: placa {vg(e['comp'])} x "
               f"{vg(e['larg'])} x {vg(e['esp'])}, USB-C {vg(e['usb_l'])} x "
               f"{vg(e['usb_h'])}. Meça antes de imprimir."),
        (None, "O comprimento da placa decide a folga sob a garra; a largura, "
               "a folga do canal."),
        (None, f"Para encaixar, cada coluna flete {vg(e['prof_canal'])} mm - "
               f"deformação de {e['eps'] * 100:.2f} %, abaixo do limite de "
               f"1,00 % adotado para o PLA."),
        (None, "A garra pode encostar nas ilhas de solda dos cantos da placa. "
               "Se atrapalhar, apare com estilete - não precisa reimprimir."),
        (None, "Com a caixa fechada os LEDs de carga não ficam visíveis. "
               "Carregue com a caixa deitada de lado: não há pés."),
        (None, "A tampa não fixa mais a perfboard - os 4 pinos saíram nesta "
               "revisão."),
    ], titulo="OBSERVAÇÕES DE PROJETO", larg=LARG_TEXTO)

    itens = (rotulos("FIX ", rebaixos)
             + [("JANELA", xj, yj, f"{vg(jy1 - jy0)} x {vg(jx1 - jx0)}")])
    tabela_furos(D, COL_DIR, y + 14, itens,
                 "COORDENADAS - origem no canto inferior esquerdo",
                 por_coluna=4)
    B.fecha()

    B.conferir()
    D.salvar("desenho-caixa-tampa.svg")
```

- [ ] **Step 6: Rode**

Run: `python gerar_modelo_3mf.py && python gerar_desenhos_3mf.py`
Expected: as três pranchas gravadas e `conferidos contra gerar_modelo_3mf.py:
nenhuma divergencia`. Se a conferência de LAYOUT reclamar, mude a ORIGEM do
bloco citado (os números `334, 110`, `334, 360`, `46, 110`) — nunca a escala de
uma vista 1:1 — até passar.

Confira que não sobrou referência:
`grep -n "PLACA\|pinos_placa\|_pino\|perfboard" gerar_desenhos_3mf.py`
Expected: só a nota "A tampa não fixa mais a perfboard".

- [ ] **Step 7: Prove que a prancha acusa divergência**

Rode, SEM regravar o `.3mf`:
`python -c "import gerar_modelo_3mf as P; P.CARREGADOR = dict(P.CARREGADOR, janela=(13.5, 7.2)); import gerar_desenhos_3mf as G; G.desenho_tampa(); G.encerrar_conferencia()"`
Expected: `AssertionError: desenho e modelo divergem` citando `tampa: janela em y`.
(O projeto mudou e a malha não — é o caso que a conferência existe para pegar.)

Depois: `git checkout -- '*.3mf' '*.stl'`. Os `.svg` entram no commit.

- [ ] **Step 8: Commit**

```bash
git add gerar_desenhos_3mf.py desenho-caixa-tampa.svg desenho-caixa-corpo.svg desenho-caixa-chave.svg
git commit -m "Redesenha a CX-03: janela USB-C e corte do berco do TP4056" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

### Task 8: Documentação, arquivos gerados e verificação final

**Files:**
- Modify: `README.md`, `LISTA-DE-MATERIAIS.md`
- Modify: `docs/superpowers/specs/2026-09-24-tampa-tp4056-design.md` (estado)
- Modify: `docs/superpowers/specs/2026-08-23-caixa-pla-corpo-painel-fundidos-design.md` (cabeçalho)
- Regenerate: `caixa-tampa.3mf`, `caixa-tampa.stl`

- [ ] **Step 1: README.md**

1. Na tabela de "Revisão atual", a linha
   `| Perfboard | não havia | **90 × 70 presa por 4 pinos, sem parafuso** |` passa a
   `| Tampa | lisa, com pinos da perfboard | **janela USB-C e berço do carregador TP4056** |`,
   e na linha `| Sólido |` troque `361,11` pelo total que
   `python gerar_modelo_3mf.py` imprimir (esperado `361,10`).
2. Substitua a seção inteira `### O encaixe da perfboard` (do título até o fim
   do bloco "Ponto de desgaste conhecido") por:

```markdown
### A entrada do carregador TP4056

A tampa recebe um módulo **TP4056 tipo C** em pé, na borda direita, com o
plugue USB-C entrando **por baixo**. Em pé porque o conector USB-C é paralelo à
placa: deitado, ele apontaria para a parede do corpo, que já está impresso e
não muda.

- **Janela** passante 13,00 × 7,20, do tamanho da capa do plugue e não do
  conector — o plugue alcança o conector qualquer que seja o balanço real dele.
- **Berço**: duas colunas em U com canal de 2,00 × 1,20. O módulo desce pelos
  canais até apoiar as pontas na chapa, e uma garra no topo de cada coluna trava
  a borda de cima. Encaixar deforma o PLA 0,78 %.
- **A caixa carrega deitada de lado** — não há pés.

> **⚠ As cotas do TP4056 são PRESUMIDAS** (placa 28,00 × 17,00 × 1,60; USB-C
> 9,00 × 3,30). Meça o módulo antes de imprimir a tampa.

> **⚠ A tampa tem lado certo.** Os 6 furos são simétricos, então ela também
> parafusa girada 180° — e aí o módulo bate no botão vermelho. Monte com a
> **janela do lado do botão verde**.
```

3. Em "Montagem — a ordem mudou", os passos 5 e 6 passam a:
   `5. TP4056 com os fios já soldados, encaixado no berço da tampa até as garras estalarem`
   `6. Tampa fechada com 6 × M3 × 6 ISO 7380, **janela do lado do botão verde**`
4. Em "O que continua faltando", no item **Passagem de cabo**, troque
   `Com a perfboard dentro, o cabo do microcontrolador não tem por onde sair.` por
   `O cabo do microcontrolador não tem por onde sair — a janela da tampa é só do carregador.`
   e acrescente os itens:
   `- **Fixação da perfboard** — os pinos saíram da tampa na revisão do TP4056.`
   `- **Posição da bateria** e passagem dos fios dela.`
5. Na tabela de arquivos: em `validar_modelo.py` troque `estação da chave e pinos`
   por `estação da chave e berço do TP4056`; a linha de `caixa-tampa.3mf` passa a
   `| caixa-tampa.3mf / .stl | Tampa de serviço, 178 × 130 × 4, com a janela USB-C e o berço do TP4056 (34,20 de altura total) |`
   (mantendo a formatação de código dos nomes); a de `desenho-caixa-tampa.svg` passa a
   `PIBIC-CX-03: furos rebaixados, janela USB-C e **corte do berço do TP4056**`.

- [ ] **Step 2: LISTA-DE-MATERIAIS.md**

1. No fim do bloco de citação do topo, acrescente:
   `>`
   `> **Revisão da tampa (24/09/2026):** entra o carregador **TP4056 tipo C**; saem os pinos da perfboard. **Só a tampa é reimpressa.**`
2. Substitua a seção `### 5b. Perfboard e o encaixe dela` (até o fim do bloco
   de citação dela) por:

```markdown
### 5b. Carregador TP4056 tipo C

| Qtd | Item | Status | Observação |
|---|---|---|---|
| 1 | **Módulo TP4056 tipo C**, com proteção (DW01 + 8205A) | ⚠ | cotas presumidas — medir |

Encaixa **em pé** no berço da tampa, sem cola nem parafuso: desce pelos canais
até apoiar na chapa, e as garras travam a borda de cima.

> **⚠ Placa 28,00 × 17,00 × 1,60 e USB-C 9,00 × 3,30 são PRESUMIDOS.** Meça
> antes de imprimir a tampa: o comprimento decide a folga sob a garra; a
> largura, a folga do canal. Os valores são parâmetro em `CARREGADOR`, no
> `gerar_modelo_3mf.py`.

> **Nota térmica.** Carregando a 1 A, o CI chega a 70–90 °C, e o PLA amolece
> por volta de 55–60 °C. O berço só toca as bordas da placa, mas a caixa é
> fechada. Se esquentar demais, troque o resistor R_PROG por um de valor maior
> — a corrente de carga cai na mesma proporção.

A perfboard 7 × 9 **perdeu a fixação**: os pinos saíram da tampa.
```

3. No bloco de volumes, `caixa-tampa       81,98 cm3     com os 4 pinos da perfboard`
   passa a `caixa-tampa       81,97 cm3     com o berço do TP4056`, e
   `sólido           361,11 cm3` passa ao total impresso (esperado `361,10`).
   Logo depois do parágrafo "A 4 perímetros…", acrescente:
   `**Reimpressão só da tampa:** ≈ 41 cm³ ≈ 51 g de PLA, pela mesma conta.`
4. Em "Impressão na A1 mini", `a tampa com os **pinos para cima**` passa a
   `a tampa com a **face externa na mesa e o berço para cima**`.
5. Em "O que trava a compra agora", apague o item 4 (perfboard) e acrescente,
   junto das outras linhas riscadas:
   `~~Ø do furo e recuo da perfboard~~ — **sem objeto:** os pinos saíram da tampa.`
6. Em "Ainda por confirmar na bancada", a linha
   `- **Ø e recuo do furo da perfboard.** Presumidos em 3,00 e 3,50.` passa a
   `- **Dimensões do TP4056 tipo C.** Presumidas: placa 28,00 × 17,00 × 1,60, USB-C 9,00 × 3,30.`

- [ ] **Step 3: Specs**

- Em `2026-09-24-tampa-tp4056-design.md`, `Estado: APROVADA, não implementada.`
  passa a `Estado: IMPLEMENTADA.`
- Em `2026-08-23-caixa-pla-corpo-painel-fundidos-design.md`, acrescente
  depois da linha `Estado: ...`:
  `A §3.5 (encaixe da perfboard) e as regras dos pinos foram removidas pela spec 2026-09-24-tampa-tp4056-design.md.`

- [ ] **Step 4: Regere e verifique tudo**

```bash
python gerar_modelo_3mf.py
python validar_modelo.py
python gerar_desenhos_3mf.py
python -m pytest -q
git status --short
```

Expected:
- `gerar_modelo_3mf.py`: `caixa-tampa ... 81,97 cm3`, total `361,10`.
- `validar_modelo.py`: `todas as conferencias passaram`.
- `gerar_desenhos_3mf.py`: `nenhuma divergencia`.
- `pytest`: `35 passed`.
- `git status`: `caixa-corpo.3mf`/`.stl` podem aparecer modificados só por
  metadado. Prove com `python -m pytest -q tests/test_corpo_intocado.py`
  (`2 passed`) e DESCARTE: `git checkout -- caixa-corpo.3mf caixa-corpo.stl`.
  O corpo impresso não recebe arquivo novo.

- [ ] **Step 5: Commit**

```bash
git add README.md LISTA-DE-MATERIAIS.md docs/superpowers/specs caixa-tampa.3mf caixa-tampa.stl desenho-caixa-tampa.svg
git commit -m "Documenta a tampa do TP4056 e regrava a tampa" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```
